# Google Container Registry API Configuration
# Copy this file to .env and fill in your actual values

# GCR API Base URL
# Examples:
# - https://gcr.io/v2 (for gcr.io)
# - https://us.gcr.io/v2 (for regional registry)
# - https://eu.gcr.io/v2 (for EU registry)
VITE_GCR_API_URL=https://gcr.io/v2

# GCR Project ID / Repository Name
# This could be your GCP project ID or the specific repository path
# Example: my-project-id or my-project-id/my-app
VITE_GCR_PROJECT_ID=your-project-id

# GCR API Key or OAuth2 Access Token
# Note: GCR typically requires OAuth2 access tokens from Google Cloud
# You can generate one using: gcloud auth print-access-token
# Or use a service account JSON key file (not recommended for frontend)
VITE_GCR_API_KEY=your-oauth2-access-token

# Optional: Service Account Email (if using service account authentication)
VITE_GCR_SERVICE_ACCOUNT=your-service-account@your-project.iam.gserviceaccount.com
